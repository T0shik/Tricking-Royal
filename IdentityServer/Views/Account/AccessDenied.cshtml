@inject Library Library

@{
    var context = await Library.GetContext();
    var tDenied = context.Read("Account", "Denied");
    ViewData["Title"] = tDenied;
}

<header>
    <h1 class="text-danger">@tDenied</h1>
    <p class="text-danger">@context.Read("Account", "DeniedDesc"))</p>
</header>
